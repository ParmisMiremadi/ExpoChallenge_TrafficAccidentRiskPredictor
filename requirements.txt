# RoadGuard AI (webapp_v3) — dependencies to RUN the app.
# Tested with Python 3.12. Install with:  pip install -r requirements.txt
flask>=3.0
flask-cors>=4.0
xgboost>=2.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24,<2.0

# Optional — only needed to REBUILD road features from the raw shapefile.
# The app ships with the prebuilt artifact, so it is NOT required to run.
# pyshp>=2.3
