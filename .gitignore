# Secrets — never commit real keys
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Large data & caches — regenerated by the pipeline, never committed
backend/pipeline/output/
backend/pipeline/weather_cache/
*.zip

# OS
.DS_Store
Thumbs.db

# Claude Code local tooling config — not project config
.claude/
